[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true
ffi = false
fs_permissions = [{ access = "read-write", path = "./reports" }]
gas_reports = []

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
